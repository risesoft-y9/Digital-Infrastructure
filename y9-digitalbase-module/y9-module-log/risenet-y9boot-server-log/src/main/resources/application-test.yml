spring:
    cloud:
        nacos:
            config:
                enabled: true
                namespace: ${NACOS_NAMESPACE:test}
                group: DEFAULT_GROUP
                server-addr: ${NACOS_SERVER_ADDR:http://nacos:8848}
                username: ${NACOS_USERNAME:nacos}
                password: ${NACOS_PASSWORD:111111}
    config:
        import:
            - optional:nacos:log.yml