spring:
    application:
        name: sso
    profiles:
        active: test
    cloud:
        enabled: true
        nacos:
            config:
                enabled: true
                group: DEFAULT_GROUP
                namespace: test
                server-addr: nacos:8848
                username: nacos
                password: Risesoft@2024
                extension-configs:
                    -   dataId: sso.yml