1、 pom.xml的修改：

  1.1、java.version全部修改为：<java.version>17</java.version>
	
  1.2、 <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
		替换为：
		<dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
		
  1.3、 <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>druid-spring-boot-starter</artifactId>
        </dependency>
		替换为：
		<dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>druid-spring-boot-3-starter</artifactId>
        </dependency>
  1.4
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>jstl</artifactId>
        </dependency>
        替换为：
        <dependency>
          <groupId>jakarta.servlet.jsp.jstl</groupId>
          <artifactId>jakarta.servlet.jsp.jstl-api</artifactId>
        </dependency>
  1.5
        <dependency>
            <groupId>javax.annotation</groupId>
            <artifactId>javax.annotation-api</artifactId>
        </dependency>
        替换为：
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
        </dependency>
2、Java源代码的修改：

  2.1、删除： src/main/resources/META-INF/spring.factories
       启用： src/main/resources/META-INF/org.springframework.boot.autoconfigure.AutoConfiguration.imports

  2.2、由于不再使用cxf组件，删除代码中的所有行：import javax.ws.rs.Path
  
  2.3、javax.validation. 全部替换为： jakarta.validation.
  
  2.4、javax.servlet. 全部替换为： jakarta.servlet. 

  2.5、javax.annotation.PostConstruct 全部替换为： jakarta.annotation.PostConstruct
   
  2.6、javax.persistence. 全部替换为： jakarta.persistence.

  2.7、com.alibaba.druid.spring.boot. 全部替换为： com.alibaba.druid.spring.boot3.
  
  2.8、实体类上的注释：@org.hibernate.annotations.Table(comment 全部替换为： @Comment("")

  2.9、实体类字段上的注解： @Type(type = "numeric_boolean") 全部替换为： @Convert(converter = org.hibernate.type.NumericBooleanConverter.class)
  
  3.0 升级后 elasticsearch 变化较大，为过编译打包，已经将原代码注释，需要修改的地方已加了注释方便找到 // FIXME elasticsearch 
    
